# Never commit credentials/history if you ever copy them in:
.credentials.json
history.jsonl
stats-cache.json

# Artifacts dropped by the `npx skills` CLI if run inside this repo
.agents/
skills-lock.json
